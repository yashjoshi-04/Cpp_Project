#ifndef UTILS_H
#define UTILS_H

#include <vector>
#include <string>
#include <sstream> // Required for std::stringstream in the implementation

// Function declaration
void splitString(std::vector<std::string>& output, const std::string& inputLine, char separator);

#endif // UTILS_H
